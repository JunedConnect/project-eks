apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-apps
  namespace: flux-system
spec:
  interval: 10m0s
  path: /flux-apps/to-do-app/overlay-local
  prune: true
  sourceRef:
    kind: GitRepository
    name: project-eks

---

apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-infra
  namespace: flux-system
spec:
  interval: 10m0s
  path: /flux-infra/overlay-local
  prune: true
  sourceRef:
    kind: GitRepository
    name: project-eks